[Основы Linux – Stepik](https://stepik.org/course/762/info) обязательно пройти этот бесплатный курс
![[Начальные уроки Linux 1.docx]]
Что такое Linux в широком смысле? - Семейство UNIX-подобных ОС, построенных на базе ядра Linux



==Работа с файлами==

1. touch - создание пустых файлов или их изменение 

2. ls - просмотр содержимого директории 
3. pwd - показать полный путь текущей директории
4. rm - удаление файла или файлов 
5. mkdir - создание директории 
6. rkdir - удаление директории 
7. cp - копирование файлов и директорий
8. mv - перемещение файлов и директории

==Работа с текстом== 

Cat - просмотр содержимого файла 
tac - просмотр содержимого файла, начиная с последней строки 
head - вывод первых строк
tail - вывод последних строк 
more - просмотр содержимого файла "в одном экране"
less -  просмотр содержимого файла с прокруткой 
grep - поиск строк по образцу
sort - сортировка строк 
wc - подсчёт строк, строк и букв 




==Выполненные задачи:==

Напишите команду для создания структуры директорий /home/box/foo/bar/baz, если вы находитесь в домашней директории (/home/box). 	mkdir -p foo/bar/baz) 
```bush
mkdir -p foo/bar/baz
```





# Введение в Ядро Linux


Основные задачи выполнения Ядра
1) Управление памятью
2) управление процессами
3) функционирование драйверов устройств
4) Системные вызовы
5) Безопасность 


Версия LInux
```bush версия Linux
uname -r 
```

Вывод буфера сообщений ядра 
```bush
dmesg
```

Запросы в базу с информацией об устройствах
```bush
udevadm info -n sdc
```

Информация о PCI-шинах и устройствах на них (Сетевые карты, видеокарты и т.д. которые подключены на материнской карте )
```bush
ispci
```

Информация о дисках и их разделах 
```bush
lsblk
```

Просмотр информации об архитектуре процессора
```bush
lscpu
```

Вывод диапазона доступной памяти и их статусы

```bush
lsmem
free - показывает статистику мпользуемой и свободной памяти
```

Показывает детальную информацию о компонентах компьютера
```bush
lsshw
```



# Типы менеджеров Пакетов
1) DPKG
2) APT
3) APT-GET
4) RPM
5) YUM
6) DNF

RPM (.rpm) напрямую не работает с зависимостями, приходится при установке делать все самому 

Установка пакета
```bash
rpm -ivh далее название пакета 
```
Удаление пакета
```bash
rpm -e telnet 
```

Обновление  пакета
```bash
rpm -Uvh telnet.rpm
```

Информация  пакета
```bash
rpm -q telnet.rpm
```

Проверка  пакета
```bash
rpm -V telnet.rpm
```



# Особенности BUSH 

File Sizes - размер файла

```bash
du -sh можно написать и название файла
```

```bash
ls -lh
```

Архивация файлов
```bash
tar -cf
```

Извлечение файлов
```bash
tar -tf
```

Сжатие файлов через bzip2, gzip, zx
```bash
bzip2 указать файл 
```

Распаковка файлов  bunzip2, gunzip, unxz
```bash
bunzip2 указать файл 
```

Просмотр сжатых файлов не распаковывая zcat, bzcat, xzcat


# Поиск файлов и директорий

Найти файл
```bash
locate prica.txt
```

Обновить базу данных 
```bash
updatedb
```

# Текстовые редакторы

VI редактор

Команда  открывает файл
```bash
vi or vim
```

Копирование, удалить, вставить, удалить отдельный символ строки в редакторе Vi
```bash
`yy` - для копирования строки  
`dd` - удалить  
`p` - вставить  
`x` - удалить отдельный символ
`u` - отмена изменения 
```


# Доступ и Права

Linux Security (Безопасность)
 1) Access Controls (Контроль доступа)
 2) Network Security (Сетевая безопасность)
 3) SELinux(Повышенная безопасность)
 4) PAM(Подключение модули аутентификации)
 5) SSH Hardening (Улучшение защиты SSH)

Информация о пользователи 
```bash
id max
```
Информация о пользователи который работает данный момент 
```bash
who
```
Активность пользователя
```bash
w
```
 Полная инфа 
<<<<<<< HEAD
=======
 
last

>>>>>>> 5a7db52b0dbf4ccffef3fd2fcfc6b0a15e81c0a8
#### **`/etc/passwd - База данных пользователей Linux** — текстовый файл со списком **всех учёток системы.`**

sudo grep alena /etc/passwd - узнать можно полное имя пользователя


Создание имя пользователя
```bash
sudo useradd diego
```
Пароль для пользователя
```bush
sudo passwd diego
```

 ![[Pasted image 20260106212138.png]]

Удаление пользователя 
```bush
userdel diego
```


# Разрешение файла

```bush
chmod <permissions> file изменение прав доступа к файлам и каталогам
```

```bush
 chmod u+rwx my-file полный доступ для owner
```

```bush
 chmod ugor+r-x my-file дать чтение для owner, group и other, убрать выполнение
```

```bush
 chmod o-rwx my-file убрать весь доступ для other
```


---

# ПРАКТИКА 


1) Какие разрешения установлены для `группы`(`group`) пользователей для той же директории `games`?
   Ответ:  Запусти: `ls -ld /home/max/games` и посмотри разрешения, установленные для группы.  
          `r-x` означает, что у группы есть разрешения на `чтение и выполнение`

2) Какие права установлены для `других`(`others`) пользователей в каталоге games?
    Ответ:  Запусти: `ls -ld /home/max/games` и посмотри разрешения, установленные для других (`others`).  
          `---` означает, что не установлено разрешений для других!

3) В каталоге games был создан новый файл с именем `residentevil`
   Он имеет полные разрешения, обнови файл, чтобы группа и другие пользователи имели только разрешения на чтение и выполнение
```bush
 chmod 755 /home/max/games/residentevil 
```

6) Теперь, добавь обратно разрешение на `запись` для `группы` и удали все разрешения для `других` для того же файла с названием `residentevil`
```bush
 chmod 770 /home/max/games/residentevil
```

7) Теперь измени владельца файла с именем `residentevil` на `service account` с названием `pollme`
```bush
 sudo chown pollme /home/max/games/residentevil
```

9) Мы создали еще один файл в каталоге игр. Измени владельца для всего каталога `games`, включая все файлы внутри, на служебную учетную запись `pollme`
   Попробуй сделать это с помощью единственной команды. Изучи рекурсивный флаг `-R` в команде `chown`
```bush
  sudo chown -R pollme /home/max/games
```


---


SSH 

Генерация ключа в SSH c типом RSA

```bush
 max@app01 ▶ ssh-keygen -t rsa
```



---


# Сравнение файлов 

```bush
diff file1 file2
```

# Сравнение директорий

```bush
diff -r ~/Desktop ~/Code

Опция `-r` (recursive) указывает `diff` сравнивать также и все вложенные поддиректории. `~/Desktop` и `~/Code` — это пути к двум сравниваемым папкам.
```



# Изменение владельца файла

```bush
ls -l example.txt



Результат:
-rw-rw-r-- 1 labex labex 0 Jul 29 15:11 example.txt

Появление: 

-rw-rw-r-- представляет права доступа к файлу (мы подробнее рассмотрим это в шаге 4). Первый символ указывает тип файла (- для обычного файла, d для каталога и т.д.). Оставшиеся символы представляют права на чтение, запись и выполнение для владельца, группы и остальных пользователей.
Первый labex — это текущий владелец файла. Это имя пользователя, которому принадлежит файл.
Второй labex — это текущая группа файла. Группа — это набор пользователей, которые могут совместно использовать права доступа.
0 — размер файла в байтах. Поскольку файл пуст, его размер равен нулю.
Jul 29 15:11 — дата и время последнего изменения.
example.txt — имя файла.





sudo chown root:root example.txt

Появнение: 
Теперь изменим владельца файла на пользователя root. root — это учетная запись администратора в системах Linux, обладающая особыми привилегиями.



Также тут идет сразу root который первый - это назначается владелей файла, а второй root это назначение группой-владельца 


```



# Изменение прав доступа к файлу


```bush
-rw-rw-r-- 1 root root 0 Jul 29 15:11 example.txt


														 Пояснение: 

Часть `-rw-rw-r--` представляет права доступа к файлу. Здесь вступают в силу числовые и символьные обозначения. Давайте разберем их:

- Первый символ (`-`) указывает, что это обычный файл. Другие распространенные индикаторы: `d` для каталога и `l` для символической ссылки.
- Следующие три символа (`rw-`) представляют права владельца (чтение и запись, но не выполнение).
    - `r` означает право на чтение: владелец может открыть и прочитать файл.
    - `w` означает право на запись: владелец может изменить файл.
    - `x` означает право на выполнение: владелец может запустить файл (если это программа или скрипт). Символ `-` означает, что в праве отказано.
- Следующие три (`rw-`) относятся к группе. Они имеют то же значение, что и выше, но применяются к членам группы файла.
- Последние три (`r--`) относятся к остальным (всем остальным пользователям). Они также имеют то же значение, но применяются к пользователям, которые не являются ни владельцем, ни членами группы файла.
  
  
  

  
  
sudo chmod 700 example.txt
  
  
															Пояснение:

  
В этой команде:

- `700` — это числовое представление прав:
    - Первая цифра (`7`) представляет права владельца.
    - Вторая цифра (`0`) представляет права группы.
    - Третья цифра (`0`) представляет права остальных.

Каждая цифра — это число от 0 до 7, вычисляемое путем сложения значений прав на чтение (4), запись (2) и выполнение (1):

- `4`: Право на чтение
- `2`: Право на запись
- `1`: Право на выполнение
- `0`: Нет прав

Таким образом, `7` (первая цифра) дает владельцу права на чтение (4), запись (2) и выполнение (1): 4+2+1=7.  
`0` (вторая цифра) не дает группе никаких прав (0+0+0=0).  
`0` (третья цифра) не дает остальным никаких прав (0+0+0=0).

Следовательно, `700` означает: Владелец: чтение, запись, выполнение. Группа: нет. Остальные: нет.
  

```


# Изменение прав доступа к каталогу

```bush

drwx------ 2 labex labex 4096 Jul 29 15:45 /home/labex/test-dir


																Пояснение:


Буква `d` в начале указывает, что это каталог. `rwx------` указывает, что владелец имеет права на чтение, запись и выполнение, а группа и остальные — нет. Для каталогов:

- Право на чтение (`r`) позволяет просматривать содержимое каталога с помощью `ls`.
- Право на запись (`w`) позволяет создавать новые файлы и подкаталоги внутри каталога.
- Право на выполнение (`x`) позволяет получать доступ к файлам и подкаталогам внутри каталога (т.е. переходить в него с помощью `cd`).



chmod -R 755 ~/test-dir

																Пояснение: 

В этой команде:

- `-R` применяет изменение рекурсивно ко всем файлам и подкаталогам (хотя в данном случае наш каталог пуст). Хорошей практикой считается включать эту опцию при работе с каталогами, даже если они сейчас пусты, на случай, если вы добавите файлы позже.
- `755` дает права на чтение, запись и выполнение владельцу, а права на чтение и выполнение — группе и остальным.

Разберем `755`:

- Владелец (7): Чтение (4) + Запись (2) + Выполнение (1) = 7
- Группа (5): Чтение (4) + Выполнение (1) = 5
- Остальные (5): Чтение (4) + Выполнение (1) = 5
  
  
  
  
Более понятное действие для предоставление доступа

## Символьный способ (рекомендуемый для наглядности):
chmod u=rwx,g=rw,o= target_file

## Числовой способ:
chmod 760 target_file

															Пояснение:

- **7** (владелец) = 4+2+1 = **rwx**
    
- **6** (группа) = 4+2+0 = **rw-**
    
- **0** (остальные) = 0+0+0 = **---**


```


# Создание нового пользователя

```bush
sudo useradd joker


Чтобы убедиться, что пользователь создан, проверим файл `/etc/passwd`

sudo grep -w 'joker' /etc/passwd

															Результат			
joker:x:5001:5001::/home/joker:/bin/sh

															Пояснение:
Имя пользователя: joker
Пароль: x (настоящий пароль зашифрован и хранится в другом месте)
ID пользователя (UID): 5001
ID группы (GID): 5001
Домашний каталог: /home/joker (пока еще не создан физически)
Оболочка (Shell) по умолчанию: /bin/sh

```

















Переход на сетевую папку 

smb://vnrapnfl03/



![[Pasted image 20260204113313.png]]